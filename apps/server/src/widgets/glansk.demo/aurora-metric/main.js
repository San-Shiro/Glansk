Glansk.connect().then(widget=>{widget.onConfig(config=>{for(const [key,value] of Object.entries(config.theme||{}))if(key.startsWith('--canvas-'))document.documentElement.style.setProperty(key,String(value));const values=Array.isArray(config.values)?config.values.slice(0,24):[24,38,31,52,49,70,64,82];document.querySelector('#eyebrow').textContent=String(config.eyebrow||'LIVE SIGNAL');document.querySelector('#value').textContent=String(config.value??'84.2');document.querySelector('#unit').textContent=String(config.unit||'%');document.querySelector('#label').textContent=String(config.label||'Performance');document.querySelector('#detail').textContent=String(config.detail||'Healthy trajectory');document.querySelector('#trend').textContent=String(config.trend||'↗ 12.4%');document.querySelector('#spark').innerHTML=values.map(value=>`<i style="height:${Math.max(5,Math.min(100,Number(value)||0))}%"></i>`).join('')});widget.reportReady()});
